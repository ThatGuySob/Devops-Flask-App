apiVersion: v1
kind: Service
metadata:
  name: the-redis-service
spec:
  type: ClusterIP
  selector:
    app: redispod
  ports:
    - name: http
      protocol: TCP
      port: 6379
      targetPort: 6379